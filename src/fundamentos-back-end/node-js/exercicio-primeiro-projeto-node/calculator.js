// ## Exercício

// - Crie um arquivo chamado `calculator.js`.
// - Escreva um função `add`que recebe dois números como parâmetro e retorna a soma deles.
// - Imprima o resultado da soma de dois números de exemplo usando `console.log`.

const add = (num1, num2) => num1 + num2;
console.log(add(4, 54));

// ---------------------

function add1(num3, num4) {
  return num3 + num4;
}

const soma = add1(3, 15);
console.log(soma);
